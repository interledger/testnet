# You can copy this file to .env.local and adjust values to run the boutique frontend locally.
# If you create both .env.local and .env, values in .env override values from .env.local.

########## FRONTEND API AND THEME ################################################################
## VITE_* variables are exposed to the browser at build time.

# VITE_API_BASE_URL=https://api.boutique.test
# VITE_THEME=light
# VITE_CURRENCY=USD