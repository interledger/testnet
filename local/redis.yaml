services:
  redis:
    container_name: redis
    image: 'redis:7-alpine'
    restart: unless-stopped
    networks:
      - testnet
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
