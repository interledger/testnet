auto-install-peers=true
engine-strict=true

# The published @interledger/hsm-atalla-* packages depend on @buf/* generated protobuf
# code, which is served by the Buf Schema Registry rather than npm. Without this line
# those transitive dependencies resolve against npmjs.org and 404.
@buf:registry=https://buf.build/gen/npm/v1/
