auto-install-peers=true
engine-strict=true

# The @interledger/hsm-atalla-* packages depend on @buf/* protobuf code.
# The Buf Schema Registry serves that code. npm does not.
# Do not remove this line. Without it, npm returns 404 for each @buf/* package.
@buf:registry=https://buf.build/gen/npm/v1/
